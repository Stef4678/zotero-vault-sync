{
  "name": "obsidian-zotero-mirror",
  "version": "0.1.0",
  "description": "Mirror your Zotero library into a git-friendly folder inside the vault (one JSON file per item, per-PDF annotation files) and generate regenerable note views over the mirror.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production",
    "typecheck": "tsc -noEmit -skipLibCheck",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "zotero"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "typescript": "^5.6.0"
  }
}
